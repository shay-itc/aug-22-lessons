import './App.css';
import Navbar from './components/Navbar';
import LoadingPage from './LoadingPage';

function App() {

  return (
    <>

      <Navbar />
      <LoadingPage />

    </>
  );
}

export default App;



// Create a navigation bar component, it should contain menu items with a drop down menu

// Along side the menu items it should have 2 buttons – “add item“ and “toggle mode”
